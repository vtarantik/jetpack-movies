apply from: "${project.rootDir}/common-lib.gradle"
android {
    namespace 'com.tarantik.jetpackmovies.feature.authentication'

    buildFeatures {
        buildConfig = true
    }
}

dependencies {
    implementation project(':core:base')

}
