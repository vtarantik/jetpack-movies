apply from: "${project.rootDir}/common-lib.gradle"
android {
    namespace 'com.tarantik.jetpackmovies.data.movieslist'

    buildFeatures {
        buildConfig = true
    }
}

dependencies {
    implementation project(':core:base')
    implementation project(':core:network')

    implementation project(':domain:movies')
}
