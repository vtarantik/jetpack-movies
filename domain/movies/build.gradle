apply from: "${project.rootDir}/common-lib.gradle"
android {
    namespace 'com.tarantik.jetpackmovies.domain.movieslist'

    buildFeatures {
        buildConfig = true
    }
}

dependencies {
    implementation project(':core:base')
}
